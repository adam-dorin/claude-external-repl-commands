node_modules/
smoke.log

# Rust build output. Prebuilt .node binaries are produced by CI (not committed yet).
native/target/
native/*.node

# legacy prototype (original Stop-hook /clear use case), archived out of the way
old/
